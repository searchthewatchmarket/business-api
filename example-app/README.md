# data-example

Vanilla JS + Node.js example consumer for the Search The Watch Market `/data` API.

## Start

```bash
npm install
npm start
```

Then open:

- App UI: `http://localhost:6006/app`
- Health: `http://localhost:6006/`
